# update the package manager and all its already installed contents
apt-get update && apt-get upgrade -y

# install gcc compiler for c language
# build-essential is a package with a bunch of programs standard to debian
apt-get install build-essential gcc git -y