# pull alpine container from dockerhub
docker pull alpine

# run the alpine container's lightweight shell
docker run -it --rm alpine /bin/ash

# The (-it) option instructs Docker to allocate a pseudo-TTY connected to the container’s stdin; 
# creating an interactive bash shell in the container
# the (--rm) option automatically removes the container when it exits 